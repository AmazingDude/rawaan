# Build Plans

Multi-step work gets a plan file here before code is written (per `AGENTS.md` and the `writing-plans` / `planning-with-files` skills).

Naming: `YYYY-MM-DD-feature-name.md`

Each plan states: goal, files touched, step-by-step tasks with verification commands, and rollback notes.
